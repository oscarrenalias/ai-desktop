{
    "OPENAI_API_KEY": "sk-xxx",
 
     "mcpServers": {
        "mcp-server-bmi": {
            "command": "docker",
            "args": [ "run", "-i", "--rm", "ghcr.io/oscarrenalias/mcp-server-bmi:latest" ]
        }
    }
}